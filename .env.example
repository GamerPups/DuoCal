# Database — local dev (SQLite)
DATABASE_URL="file:./dev.db"

# For Vercel/production, use Neon PostgreSQL instead:
# DATABASE_URL="postgresql://user:pass@ep-xxx.us-east-2.aws.neon.tech/duocal?sslmode=require"

# NextAuth — REQUIRED
NEXTAUTH_URL="http://localhost:3000"
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
NEXTAUTH_SECRET=""

# Google OAuth — REQUIRED (Google Cloud Console → OAuth 2.0 Client ID)
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# Resend (optional — email invites)
RESEND_API_KEY=""
RESEND_FROM_EMAIL="DuoCal <onboarding@resend.dev>"

# OpenAI (optional — AI scheduling)
OPENAI_API_KEY=""
